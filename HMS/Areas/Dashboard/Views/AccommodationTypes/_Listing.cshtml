@model AccommodationTypeListingModel


@if (Model != null && Model.AccommodationTypes != null && Model.AccommodationTypes.Count() > 0)
{
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4 class="text-center">Accommodation Type Table</h4>
        </div>

        <div class="panel-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr class="text-center">
                            <th>Name</th>
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var accommodationTypes in Model.AccommodationTypes)
                        {

                            <tr class="success text-center">
                                <td>@accommodationTypes.Name</td>
                                <td>@accommodationTypes.Description</td>
                                <td class="embed-responsive-item">
                                    <button class="action-btn btn btn-primary float-lg-left mr-2" data-href="@Url.Action("Action", "AccommodationTypes" ,new { ID=accommodationTypes.ID})" data-toggle="modal" data-target="#myModal"><i class="fa fa-pencil-square-o mr-1"></i>Edit</button>
                                    <button class="action-btn btn btn-danger float-lg-left" data-href="@Url.Action("Delete", "AccommodationTypes" ,new { ID=accommodationTypes.ID})" data-toggle="modal" data-target="#myModal"><i class="fa fa-trash-o mr-1"></i>Delete</button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="alert alert-warning" role="alert">
        Sorry! NO records exist.
    </div>
}
